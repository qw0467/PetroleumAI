You are continuing the PetroleumAI project.
A full MVP was already created with:

simulated multivariate sensor dataset

anomaly detection (Isolation Forest)

drift detection

spike detection

anomaly labels

KPIs

visualizations

a Streamlit UI

Now add a new module inside src/ called:

root_cause.py


This module must perform gap analysis + root cause analysis using the simulated dataset and the computed anomaly outputs.

1. PURPOSE OF THE ROOT CAUSE ANALYSIS MODULE

Given:

sensor data

drift regions

spikes

anomaly score timeline

shutdown timestamp

rolling features

The module must determine:

A. What went wrong?

Identify which sensor first behaved abnormally

Identify whether instability began due to:

pressure drift

temperature spike

flow instability

multi-sensor correlation breakdown

vibration increase (if present)

B. When did the first abnormality occur?

Output the exact timestamp

Output how far before the shutdown the first warning appeared

C. Which features contributed most to the anomaly score?

Based on reconstruction error or feature deviation

Rank sensors in order of “responsibility”

D. How did this differ from normal behaviour?

Compare abnormal region vs. baseline:

Mean deviation

Variability increase

Trend reversal or slope increase

Threshold violations

E. Final engineering-style explanation

The module must generate a human-readable diagnostic summary such as:

“The shutdown was likely caused by increasing pressure drift beginning 45 minutes before the trip, followed by flow instability and a temperature spike. Pressure P1 showed a 35% deviation from baseline, with slope reversal at T=14:32.”

This must mimic a real petroleum plant gap analysis.

2. CODE REQUIREMENTS

Inside root_cause.py, implement:

Function 1: identify_root_cause(df, anomaly_col, drift_cols, spike_cols, shutdown_time)

Returns a dict:

{
  "first_abnormal_time": "...",
  "time_before_shutdown_minutes": ...,
  "primary_causal_sensor": "...",
  "contributing_sensors": [...],
  "abnormal_patterns_summary": "...",
  "differences_from_baseline": "...",
  "final_explanation": "..."
}

Function 2: compare_to_baseline(df, abnormal_window)

Return:

baseline mean/std (normal region)

abnormal mean/std

% deviation

variability increase

Function 3: generate_engineer_report(results_dict)

Return a multi-paragraph diagnostic report covering:

timeline of instability

parameter deviations

correlation breakdown

sequence of failure events

probable root cause

recommended actions

3. INTEGRATION WITH STREAMLIT

Modify streamlit_app.py:

Add a new tab/page: “Root Cause Analysis”

User selects:

shutdown event timestamp
OR

use the automatically detected shutdown point

App displays:

First abnormal timestamp

Sensor causality ranking

Deviations vs baseline table

Root cause explanation

A final written engineering-style report

Make sure the UI does not crash even if:

No shutdown event is selected

Data is missing

Dataset is too short

4. FINAL EXPECTED OUTPUT

Return:

1. Full code for src/root_cause.py in its own code block
2. Updated sections of streamlit_app.py
3. Any modifications needed in other files
4. Example output of the diagnostic explanation
5. Instructions in README.md to document this feature