You are an AI software engineer inside a Replit environment.
Create a complete, runnable MVP (Minimum Viable Product) for a project called PetroleumAI.

This MVP must run inside Replit, with:

Python 3

pip install compatibility

A working Streamlit app

Auto-generated dataset

Modular code

Clear folder structure

Your goal is to produce a fully functioning predictive maintenance prototype for petroleum plant equipment using multivariate time-series analysis.

1. PROJECT REQUIREMENTS
A. Functionality

Build an MVP that can:

1. Simulate sensor data

Generate a CSV containing:

pressure

temperature

flow

vibration (optional)

at least one shutdown event

drift, spikes, instability patterns

2. Engineer features

Compute:

rolling mean

rolling std

slope/trend

spike detection

drift detection

cross-correlation

instability score

3. Run anomaly detection

Use Isolation Forest to compute:

anomaly score

anomaly label

thresholds

4. Produce KPIs

Generate:

Equipment Health (Normal/Warning/Critical)

Current anomaly score

Irregular event count

Drift detected per sensor

First abnormal timestamp

Overall risk indicator

5. Visualize

Create charts:

Sensor time-series + anomaly overlay

Anomaly score timeline

Drift detection chart

KPI summary

6. Streamlit UI

Streamlit app must:

Allow dataset upload OR use simulated data

Display KPIs in cards

Show all charts

Has sidebar toggles:

Show drift

Show spikes

Show anomaly overlay

Provide processed-data download button

2. REQUIRED FOLDER STRUCTURE

Inside Replit, create:

petroleumai/
  ├── data/
  │     └── simulated_data.csv
  ├── src/
  │     ├── simulate.py
  │     ├── features.py
  │     ├── model.py
  │     ├── kpis.py
  │     └── visualize.py
  ├── app/
  │     └── streamlit_app.py
  ├── requirements.txt
  └── README.md


If Replit blocks nested folders, prefix with petroleumai_ and place in root.

3. CODING RULES

All code must be runnable in Replit without modification

Use clean Python

Avoid heavy dependencies

All functions must include docstrings

Use defensive programming so the app doesn’t crash on empty uploads

Don’t use GPU-dependent libraries

4. STREAMLIT APP REQUIREMENTS

UI layout:

Home Page

Project title

“Use simulated data” button

“Upload CSV” uploader

KPIs Section

Show as metrics:

Health status

Current anomaly score

Irregular events count

Drift flags

Charts Section

Sensor charts with anomaly overlays

Anomaly timeline chart

Drift chart

Controls Sidebar

Toggle anomaly overlay

Toggle drift markers

Toggle spikes display

Download

Button to download processed dataset with anomaly labels

5. README.md Requirements

README must include:

What PetroleumAI does

How the anomaly detection works

What KPIs mean

Setup steps for Replit

How to run Streamlit inside Replit

Example screenshots placeholders

Future improvements section

6. DELIVERABLE FORMAT

You must return:

1. Full working code for all files

Each file in its own separate code block, like:

# src/simulate.py
<code here>

2. Complete requirements.txt

(include: pandas, numpy, scikit-learn, matplotlib, streamlit)

3. The README.md file in full
7. EXECUTION EXPECTATION

When the user presses Run in Replit:

The app should start Streamlit

Simulated dataset should exist

UI must load without errors